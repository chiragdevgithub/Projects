public with sharing class PrimeNumber {
	public  boolean validateNumber(int number){
	
	int i,flag=0;
				
				for(i=2; i<=n/2; ++i) {
       
       			 	if(math.mod(number,i)==0) {
           				flag=1;
            			break;
       				 }
   				 }

    if (flag==0)
       return true;
    else
       return false;
    
  
	}
}