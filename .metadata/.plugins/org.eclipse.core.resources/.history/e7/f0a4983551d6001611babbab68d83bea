public with sharing class PrimeNumber {
	public  boolean validateNumber(int no){
	
	int i,flag=0;
				
				for(i=2; i<=no/2; ++i) {
       
       			 	if(math.mod(no,i)==0) {
           				flag=1;
            			break;
       				 }
   				 }

    if (flag==0)
       return true;
    else
       return false;
    
  
	}
}